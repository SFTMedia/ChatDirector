debug: false
chains:
  - schedule-input:
      - schedule-input:
            delay: 10
      - schedule-input:
            delay: 15
            period: 100
  - schedule-cron-input:
      - schedule-cron-input:
            cron: "* *"
      - schedule-cron-input:
            cron: "* * *"